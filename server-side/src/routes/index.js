const API_ROUTES = {
  ROOT: '/',
  USER: '/user',
};

const ADMIN_ROUTES = {
  ROOT: '/admin',
  CATEGORY: '/admin/category',
};

const VIEW_ROUTES = {
  ROOT: '/',
  ADMIN: '/admin',
  CATEGORY: '/admin/category',
};

export { API_ROUTES, ADMIN_ROUTES, VIEW_ROUTES };
